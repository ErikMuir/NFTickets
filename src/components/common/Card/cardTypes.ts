export type CardProps = {
  title?: string;
  subtitle?: string;
  description?: string;
  imageUrl?: string;
  overlayText?: string;
  className?: string;
};

export type CardImageProps = {
  imageUrl: string;
  overlayText?: string;
  className?: string;
};
